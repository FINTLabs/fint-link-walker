spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/linkwalker
    username: linkwalker
    password: linkwalker

fint:
  linkwalker:
    auth:
      flais-gateway: "http://localhost:56417"
