# IDE / editor
.idea/
.vscode/
*.iml
*.iws
*.ipr
out/

# Go
# Build outputs (we always run `go build -o /tmp/...` or use Docker)
/scanner
/reader

# Local secrets and per-developer overrides
.env
.env.local
.env.*.local

# Heap profiles, coverage, traces from local pprof / go test runs
*.prof
*.pprof
*.out
coverage.txt

# Reports written by local FileStore-based runs (legacy)
reports/

# Misc
.DS_Store
